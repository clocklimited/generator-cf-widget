{
  "name": "generator-cf-widget",
  "description": "Generate scaffolding for new Catfish widgets.",
  "files": [
    "app"
  ],
  "version": "0.0.0",
  "dependencies": {
    "camel-case": "^1.1.2",
    "chalk": "^1.1.1",
    "slugg": "^0.1.2",
    "yeoman-generator": "^0.20.3"
  },
  "scripts": {
    "lint": "eslint -f unix .",
    "inspect": "jsinspect",
    "pretest": "npm run lint && npm run inspect",
    "test": "istanbul cover ./node_modules/.bin/_mocha test",
    "posttest": "(istanbul check-coverage --statements 90 --branches 90 --functions 100 --lines 90 && rm -rf coverage) || echo Look at 'coverage/lcov-report/index.html' to find out more"
  },
  "publishConfig": {
    "registry": "http://npm.clockte.ch"
  },
  "author": "Bala Clark <bala.clark@clock.co.uk>",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^1.4.1",
    "eslint-config-clock": "^1.0.0",
    "eslint-config-standard": "^4.3.2",
    "eslint-plugin-standard": "^1.3.0",
    "istanbul": "^0.3.19",
    "jsinspect": "^0.7.0",
    "mocha": "^2.3.2"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:balaclark/generator-cf-widget.git"
  }
}
